var object = raycast.get_collider()
	if raycast.is_colliding():
		if object.is_in_group("pickable"):
			if Input.is_action_pressed("interaction"):
				#sets the layer to 2 so it is picked up, so walls need to collide with layer 2 when it is being held.
				object.collision_layer = 2
				#sets it back to 1 so it can be picked up again. 
				object.collision_layer = 1
				object.global_position = hand.global_position
				object.global_rotation = hand.global_rotation
				object.linear_velocity = Vector3(0.1, 0.1, 0.1)
				